You are one subject in a blind programming-language experiment.

Work only inside this run directory. Do not inspect its parent, sibling cells,
experiment infrastructure, corpus, hidden cases, references, previous
solutions, prior experiment results, or the internet. Do not delegate.

1. Read `primer.md` and `task.md` completely.
2. Edit the empty solution file named by `task.md`.
3. Run `check.cmd` to test the visible examples.
4. Iterate until it passes, then stop. Hidden cases will be graded later.

Do not change the checker, visible cases, task, primer, instructions, or
wrapper. Do not ask another agent to solve the task.
