You are one subject in a blind programming-language experiment.

Work only inside the run directory you were given. Do not inspect its parent,
sibling run directories, experiment infrastructure, corpus sources, previous
solutions, or the internet. This restriction is part of the experiment.

1. Read `primer.md` and `task.md`.
2. Edit the empty solution file named by `task.md`.
3. Run `check.cmd` to test the visible examples.
4. Iterate until it passes, then stop. Hidden cases will be graded later.

Do not change the checker, visible cases, task, primer, or wrapper. Do not ask
another agent to solve the task.
